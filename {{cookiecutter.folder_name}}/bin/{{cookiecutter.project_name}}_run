#!/bin/bash
# insta485db

# Stop on errors
# See https://vaneyckt.io/posts/safer_bash_scripts_with_set_euxo_pipefail/
set -Eeuo pipefail


# Check for var/insta485.sqlite3
# if [ ! -f "var/insta485.sqlite3" ]; then
#   echo "Error: can't find database var/insta485.sqlite3"
#   echo "Try: ./bin/insta485db create"
#   exit 1
# fi

# Print commands
set -x

# Run server
flask --app {{cookiecutter.project_name}} --debug run --host 0.0.0.0 --port 8000
